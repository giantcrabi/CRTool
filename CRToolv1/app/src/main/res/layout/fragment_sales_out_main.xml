<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Fragment.SalesOutMainFragment">

    <TextView
        android:id="@+id/curOutlet"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:text="OUTLET A"
        android:textColor="@color/colorAccent"
        android:textSize="40sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:orientation="vertical">

        <Button
            android:id="@+id/doInputSN"
            style="@style/DarkButton"
            android:drawableLeft="@drawable/ic_touch_app_white_36dp"
            android:text="@string/input_sn" />

        <Button
            android:id="@+id/scanSN"
            style="@style/DarkButton"
            android:drawableLeft="@drawable/ic_settings_overscan_white_36dp"
            android:text="@string/scan_sn" />

    </LinearLayout>

</LinearLayout>
